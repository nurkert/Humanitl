/// Re-export of `packages/ui`.
///
/// Features import this file, never the package directly, so a later swap of
/// the component library touches one place only (CONVENTIONS 3.9).
library;

export 'package:humanitl_ui/humanitl_ui.dart';
