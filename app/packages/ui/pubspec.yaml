name: humanitl_ui
description: "Design tokens and the widget vocabulary of the application (ADR-0009)."
publish_to: "none"
version: 0.0.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.2"

dependencies:
  flutter:
    sdk: flutter
  # Die Komponentenbibliothek hinter der Naht (ADR-0009, revidiert am
  # 2026-09-04). Nur dieses Paket darf sie importieren; ein Import in einem
  # Feature ist ein Architekturverstoss und wird von `tools/check-deps.sh`
  # beanstandet. Exakt gepinnt, weil die Bibliothek jung ist und in sechs
  # Veroeffentlichungen achtzehn Breaking-Punkte hatte.
  shadcn_flutter: 0.0.54

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
