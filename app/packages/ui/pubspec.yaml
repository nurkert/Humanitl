name: humanitl_ui
description: "Design tokens and thin wrappers around the component library."
publish_to: "none"
version: 0.0.0

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
