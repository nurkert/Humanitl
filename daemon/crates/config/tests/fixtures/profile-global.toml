# Ein globales Profil. Konfigurationswerte stehen unter [config.<gruppe>].
[config.hold]
timeout_secs = 120

[config.ui]
language = "de"

# Der Regelblock eines Profils (HUM-066): Dateien relativ zur Profildatei,
# eigene Regeln im Schema von rules.yaml.
[rules]
files = []
inline = []
